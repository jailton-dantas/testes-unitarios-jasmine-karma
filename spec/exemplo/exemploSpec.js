describe("Suite de teste de exemplo com NodeJS", () => {
    it("Deve demonostrar o uso do Jasmine com Node.js", () => {
        expect(true).toEqual(true);
    });

    it("Deve demonstrar o uso do karma em modo escuta", () => {
        expect(1).toEqual(1);
    })
});